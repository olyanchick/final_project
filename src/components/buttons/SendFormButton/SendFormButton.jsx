import classes from './SendFormButton.module.css'
const SendFormButton = () => {
    return (
        <div>
            {/*<button className={classes.form_button} disabled={errors.name || errors.email || errors.password}>Отправить</button>*/}
        </div>
    );
};

export default SendFormButton;